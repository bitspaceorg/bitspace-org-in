generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model message {
  id       Int      @id @default(autoincrement())
  content  String
  parentId Int?
}
